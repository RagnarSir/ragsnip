# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.6.0.schema.json
PackageIdentifier: RagnarSir.RagSnip
PackageVersion: 1.0.1
PackageLocale: en-US
Publisher: RagnarSir
PublisherUrl: https://github.com/RagnarSir
PublisherSupportUrl: https://github.com/RagnarSir/ragsnip/issues
PackageName: RagSnip
PackageUrl: https://github.com/RagnarSir/ragsnip
# TODO: pick a license. If you add a LICENSE file (e.g. MIT) to the repo,
# change this to the SPDX identifier ("MIT", "Apache-2.0", etc.).
License: NoAssertion
ShortDescription: A tiny snipping tool that uploads region screenshots and copies the URL to the clipboard.
Description: |
  RagSnip captures a region of the screen using the built-in Windows
  Snip & Sketch overlay, uploads it to your choice of catbox.moe, 0x0.st,
  or Imgur, and puts the resulting URL on your clipboard. The persistent
  GUI keeps a thumbnail history of recent snips — left-click any row
  to re-copy the URL, right-click for a menu (Copy URL / Copy image /
  Open in browser).
Moniker: ragsnip
Tags:
  - screenshot
  - snipping
  - clipboard
  - catbox
  - imgur
  - upload
  - share
ManifestType: defaultLocale
ManifestVersion: 1.6.0
